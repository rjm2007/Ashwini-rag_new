# Extract all SRSM request types from tables — JSON only.

OUTPUT:
{
  "profiles": {
    "krones_supplier_doc": {
      "request_types": [
        {"parent_category":"", "request_type":"", "reason":"", "responsible_party":""}
      ]
    }
  }
}

Expect up to 13 request types in 3 parent categories.
