# Extract Krones requirements, standards, definitions, thresholds — JSON only.
# status on scalars inside requirements: use plain strings for list items.

OUTPUT:
{
  "profiles": {
    "krones_supplier_doc": {
      "requirements": [{"requirement":"", "category":"", "mandatory":true, "reference_standard":"", "section_no":"", "confidence":0.9}],
      "referenced_standards": [{"standard_code":"", "context":"", "section_no":""}],
      "definitions": [{"term":"", "definition":"", "section_no":""}],
      "thresholds_and_rules": [{"rule_name":"", "condition":"", "section_no":""}]
    }
  }
}

Extract QPI bands, archival periods, packaging dates, DIN/ISO standards verbatim.
