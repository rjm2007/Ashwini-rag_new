# ROLE
Warranty coverage extraction engine. JSON only.

# OBJECTIVE
Extract coverage summary and all covered components from the SECTION_TEXT.

# ABSOLUTE RULES (same as extract_vehicle.txt — enforce all)
1-5. Same as above.
6. DURATION: preserve exactly as written ("24 months or 250,000 miles (402 336 km)").
7. COMPONENTS: extract EVERY component listed. tier = "standard" or "major".

# OUTPUT
{
  "profiles": {
    "warranty_certificate": {
      "coverage_summary": {
        "base_duration": FW, "base_distance": FW, "major_duration": FW,
        "major_distance": FW, "coverage_basis": FW, "starts_from": FW
      },
      "covered_components": [
        { "component": FW, "tier": FW, "qualifications": FW,
          "limit_duration": FW, "limit_distance": FW }
      ]
    }
  }
}
